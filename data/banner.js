/* ============================================================
   BANNER — a thin strip of images at the very top of the page
   that scrolls continuously, right to left.

   Add as many image paths as you like — they'll automatically
   repeat in a smooth, seamless loop. More images = a longer
   loop before it repeats; fewer = a shorter, faster-feeling one
   (the scroll speed itself stays constant either way).

   Put the actual image files in the /images folder and list
   their relative paths here. Leave the array empty (as it is
   now) and the banner just won't show up — nothing breaks.
   ============================================================ */
window.BANNER_IMAGES = [
  // "images/banner-1.jpg",
  // "images/banner-2.jpg",
  // "images/banner-3.jpg"
];
